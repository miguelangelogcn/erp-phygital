// src/types/client.ts
export interface Client {
  id: string;
  name: string;
  // Outros campos podem ser adicionados aqui no futuro
  // email?: string;
  // phone?: string;
  // address?: string;
}